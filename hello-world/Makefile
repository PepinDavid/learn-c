.PHONY: clean All

All:
	@echo "----------Building project:[ SDL-events - Debug ]----------"
	@cd "SDL-events" && "$(MAKE)" -f  "SDL-events.mk"
clean:
	@echo "----------Cleaning project:[ SDL-events - Debug ]----------"
	@cd "SDL-events" && "$(MAKE)" -f  "SDL-events.mk" clean
